[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "theranostics-prototype"
version = "0.0.1"
description = "Local prototype for biomedical survival pipeline"
authors = [ { name = "Theranostics" } ]
dependencies = []
